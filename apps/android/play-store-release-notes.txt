<de-DE>
• Supabase-Sync behoben: Beim ersten Eintragen der Zugangsdaten wurde die Bibliothek nicht automatisch synchronisiert.
• Bibliothek zeigt jetzt einen Sync-Hinweis, wenn Supabase im Hintergrund synchronisiert (z. B. direkt nach dem Speichern der Einstellungen).
• Nicht verwendete Abhängigkeit (MLKit OCR) entfernt – kleinere App-Größe.
</de-DE>
<en-US>
• Fixed Supabase sync: the library was not automatically synced when entering credentials for the first time.
• Library now shows a sync indicator while Supabase is syncing in the background (e.g. right after saving settings).
• Removed unused dependency (MLKit OCR) – smaller app size.
</en-US>
